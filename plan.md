# 掌柜智库多智能体系统重构方案（从工作流到多智能体生态）

## 一、 现行架构的“根本性缺陷”诊断（为什么必须大改）

| 维度 | 现行问题 | 重构方向 |
| :--- | :--- | :--- |
| **意图识别** | 写死在 `item_name_confirm` 节点，硬编码提取“商品”，无法扩展新场景。 | 独立的**意图与规划层**，动态识别用户意图。 |
| **状态管理** | 仅用 `QueryGraphState` 字典传递，强依赖节点顺序，无全局共享记忆。 | 引入 **“共享黑板（Blackboard）”** 模式 + 分层状态机。 |
| **工具调用** | 工具（检索、Web搜索）硬编码在节点内部，无法动态按需组合。 | 实现 **MCP (模型上下文协议)** 标准的工具注册中心，Agent按需调度。 |
| **知识存储** | Milvus 只存商品向量，MongoDB 只存对话，未做**知识图谱化**和**多模态分类**。 | 构建**元数据标签体系**（领域、类型、时间、质量评分），实现结构化+向量混合检索。 |
| **流程编排** | 单链 DAG（有向无环图），无循环、无自我反思、无法处理复杂多步任务。 | 改为 **ReAct (Reasoning + Acting)** 或 **Plan-and-Execute** 循环范式。 |


## 二、 基于软件工程规范的“五层架构”重设计

采用 **领域驱动设计（DDD）** 分层，彻底解耦业务、智能体逻辑与基础设施。

```text
┌─────────────────────────────────────────────────────────────┐
│                   1. 表现层 (Presentation)                  │
│  统一网关 (FastAPI) + WebSocket/SSE  + 前端动态渲染        │
└─────────────────────────────────────────────────────────────┘
                              │
┌─────────────────────────────────────────────────────────────┐
│                   2. 应用层 (Application)                   │
│  API Orchestration (用例编排) + 事件发布/订阅              │
└─────────────────────────────────────────────────────────────┘
                              │
┌─────────────────────────────────────────────────────────────┐
│                   3. 领域层 (Domain) —— 核心               │
│  ┌──────────┐  ┌─────────┐  ┌─────────┐  ┌────────────┐ │
│  │ 规划器    │  │ 执行器  │  │ 反思器  │  │ 记忆管理器 │ │
│  │(Planner) │  │(Executor)│  │(Critic) │  │(Memory)    │ │
│  └──────────┘  └─────────┘  └─────────┘  └────────────┘ │
│  多智能体协作 (LangGraph 或 AutoGen 框架)                  │
└─────────────────────────────────────────────────────────────┘
                              │
┌─────────────────────────────────────────────────────────────┐
│                   4. 基础设施层 (Infrastructure)            │
│  工具注册表(MCP)  │  向量/图/关系数据库  │  文件存储      │
└─────────────────────────────────────────────────────────────┘

        用户输入
           │
           ▼
┌─────────────────────┐
│  主管智能体 (Supervisor) │  ← 思考：需要规划吗？调用哪个Worker？
└─────────────────────┘
           │ (分发任务)
    ┌──────┼──────┬──────────────┐
    ▼      ▼      ▼              ▼
┌──────┐ ┌──────┐ ┌──────────┐ ┌──────────┐
│研究者│ │生活家│ │代码工程师│ │记忆管家  │
│(Paper)│ │(Life)│ │(Dev)     │ │(Memory)  │
└──┬───┘ └──┬───┘ └─────┬────┘ └─────┬────┘
   │        │            │            │
   └────────┴─────┬──────┴────────────┘
                  ▼
         ┌───────────────────┐
         │  反思器 (Critic)   │ ← 结果是否符合预期？是否需要重试或补充？
         └───────────────────┘
                  │ (完成/答复/请求补充信息)
                  ▼
              输出给用户


前后端交互规范升级（面向 Agent 的 API）
传统的 /query + /stream 已不满足。你需要实现 “任务提交 + 状态轮询/推送” 模式。

同步快速响应（简单查询）：沿用 POST /v1/agent/chat，返回 SSE 流。

异步长任务（如“找10篇关于 RAG 的论文并总结”）：POST /v1/agent/task 返回 task_id，前端通过 GET /v1/agent/task/{task_id}/status 轮询，或通过 WebSocket 推送进度。

前端交互逻辑重构：

引入 对话-任务面板：左边是聊天历史，右边是“当前任务执行进度可视化”（类似于 AutoGPT 的步骤树）。

支持 “人机回环 (Human-in-the-loop)”：当 Agent 不确定（如外卖商家太多），它会在流中插入 type: "clarification" 和 options，前端渲染为按钮，用户点击后继续执行。


Multi-Agent 框架选型：既然已有 LangGraph 基础，继续深耕 LangGraph 是最好的选择，其 SubGraph 功能非常适合做多智能体嵌套。强烈建议搭配 LangSmith 进行链路追踪调试，这对复杂的多智能体开发至关重要（远超 print 调试）。

提示词工程升级：从“填空模板”升级为 “ReAct 风格” 与 “思维树 (Tree of Thoughts)” 提示词，让 Supervisor 能够显式输出 { "thought": "...", "action": "...", "action_input": "..." }。

避免过度工程：对于“订外卖”这类有强外部依赖的场景，初期不要追求真正的“下单”，先做 “比价推荐 + 清单生成”，下单动作预留接口但由用户点击跳转完成，避开支付合规风险。